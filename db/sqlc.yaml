version: "2"
sql:
  - engine: "postgresql"
    queries: "query/"
    schema: "migrations/000001_init_shema.up.sql"
    gen:
      go:
        package: "db"
        out: "sqlc"
        emit_json_tags: true
        emit_interface: true
        emit_empty_slices: true
